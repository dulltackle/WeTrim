# 微信公众号文章清洗

WeTrim 帮助用户逐块取舍微信公众号文章的内容，并编辑要保留的文字，得到清洗后的文章。

## 领域术语

**文章**：用户正在清洗的一篇微信公众号文章，包含正文及标题、公众号名称等来源信息。

**微信提示页**：微信自己返回的、说明一篇文章为何无法阅读的页面。WeTrim 原样转述它给出的原因，不自行推断删除、违规、登录或验证等具体原因。
_避免使用_：把「读不到正文」一律称为文章已删除。

**文章快照**：一次抓取所取得的文章内容及来源信息。同一文章再次抓取得到另一份快照。

**候选快照**：已经抓取并保存、但尚未被用户接受为当前清洗会话所依据的那一份文章快照。它不承载任何清洗进度，可以被另一次抓取覆盖，也可以被丢弃。
_避免使用_：用候选快照指代当前清洗会话正在清洗的快照。

**清洗会话**：围绕一份文章快照进行的内容取舍和编辑，以及这些操作留下的当前进度。
_避免使用_：文章库、历史记录。

**内容块**：清洗时可以独立选择保留或剔除的正文单位。列表和引用各自整体构成一个内容块；普通段落中的文字与图片按原顺序分别构成内容块。
_避免使用_：把 DOM 节点、段落或图片当作所有内容块的统称。

**块类型**：内容块在首次切分时的内容类别，包括段落、标题、图片、代码、列表、引用、表格、分割线、独立公式、富媒体及未知内容。编辑不会重新切分内容块，也不会改变其初始类别。

**块身份**：同一份文章快照内，用于持续识别一个内容块的身份。内容、取舍或位置描述的变化不改变它，另一份快照中的块不自动视为同一个块。

**保留 / 剔除**：用户对一个内容块是否进入清洗结果的选择。

**初始内容**：内容块首次转换得到、供编辑和还原使用的内容。

**当前内容**：用户编辑后的内容；尚未编辑或已还原时，等于初始内容。

**还原内容**：将一个内容块的当前内容恢复为初始内容，不改变其保留或剔除状态。

**清洗结果**：按文章原顺序汇集保留块的当前内容所得的正文；剔除块与空白块不进入结果。

**导出产物**：一次导出所生成的文章文件及其引用的本地图片。经用户明确选择，可以保留未下载图片的外链；这类产物不保证离线完整。
_避免使用_：用导出产物指代可恢复编辑进度的清洗会话。

**图片块**：以一张图片为内容、可以独立选择保留或剔除的内容块。

**图片引用**：当前内容对一张图片的使用。同一张图片可被多个内容块引用，列表、引用等块内部的图片引用也属于所在块的内容。

**图片资源**：文章中一张图片所对应的资源，可供多个图片引用共同使用。它是否仍被保留内容引用，与某一个图片块是否被剔除是不同的问题。
_避免使用_：用图片块统称图片资源。

**富媒体块**：承载公众号名片、视频、音频或小程序卡片等内容的块。无法完整表达时，保留可读信息或说明原内容的占位文字，供用户取舍。

**未知内容块**：尚不能识别其内容类别的块。以可读信息或占位说明呈现，供用户决定保留或剔除。
