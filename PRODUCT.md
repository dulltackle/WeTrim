# WeTrim 产品事实

<!-- impeccable:product-schema 1 -->

本文件是 WeTrim 的产品事实记录：产品层的决定、约束、证据与已知边界都在这里写全。领域术语以根目录 [`CONTEXT.md`](./CONTEXT.md) 为准，本文件加粗引用但不重新定义；除它之外，理解本文件不需要读其他文档。

所有决定均来自[决策地图](https://github.com/dulltackle/WeTrim/issues/1)下已关闭的 13 张决策票；正文中的 issue 与 ADR 链接是论证出处，供追溯用，**不是阅读本文件的前置条件**。

实现层的契约——架构分层、数据流、数据结构、消息协议、存储契约、源码结构与实现期必测清单——在 [`ARCHITECTURE.md`](./ARCHITECTURE.md)。


## Platform

web

## Stack

自包含的 Chrome / Edge 浏览器扩展，**单独一份 MV3 包**，上架 Chrome Web Store。

| 用途 | 选型 |
| --- | --- |
| 构建 | Vite + TypeScript |
| 界面 | React 19 |
| HTML → Markdown | Turndown + `turndown-plugin-gfm` |
| Markdown 预览 | marked + DOMPurify |
| 状态管理 | 单个 `useReducer` + Context，不引入外部 store |
| 打包/压缩 | 无（JSZip 不引入） |
| 写盘 | File System Access API（`showDirectoryPicker`），不声明 `downloads` 权限 |

- MV3 默认 CSP（`script-src 'self'; object-src 'self'`）**原样保留，不放宽**：禁远程脚本、内联脚本、`eval`、WebAssembly，第三方库必须打包进扩展。商店规则同时禁止代码混淆。
- ✅ 上述第三方库（React 19、Turndown、`turndown-plugin-gfm`、marked、DOMPurify）的生产构建**已实测完全不依赖 `eval`**：在 Chrome 153 MV3 严格 CSP（`script-src 'self'; object-src 'self'`）下无违规，全部运行正常（Issue #15 实测验证）。
- 清洗交互原型是独立静态 HTML，只用于决策，**不合入正式扩展**；正式实现用 React 重写。

## Users

**完全不懂技术的人**，希望手动清洗自己正在读的微信公众号文章。他们不会、也不应该需要安装 Python、Node 或任何本机服务。

使用场景：用户在浏览器里已经打开并读到了一篇公众号文章（登录、验证等前置状态都已由他们自己在正常浏览中完成），想把其中真正有价值的部分留下来，变成可归档、可再编辑的 Markdown。

## Product Purpose

按阅读顺序通读一篇文章，逐块选择**保留 / 剔除**，必要时编辑保留内容，检查结果后导出 Markdown 和图片。

成功的标准是：用户拿到的导出产物只包含他自己拍板留下的内容，格式忠实、图片在本地可用；过程中任何一步失败都没有毁掉他已经付出的取舍和编辑。

## Positioning

**零安装门槛是唯一的硬约束，其余全部选择都从它推出来。**

- 浏览器扩展形态本身把反爬风险归零——正文来自用户已经打开的那个标签页，扩展不自己去取页面。商店一键安装，分发成本 $5 一次性。
- **不是任何 CLI 的前端配套。** 不依赖本机安装 Python、Node 或任何服务；上游 Python CLI / Camoufox / 本地 Node 服务路线已随架构改换整体作废（`mvp.md` 已删除）。
- **v1 只有一个文章入口**：用户在自己已打开的公众号文章标签页上点击扩展图标。不提供粘贴 URL 的入口——粘贴意味着扩展要自己去取页面，等于把反爬赛道原样请回来（2026-09-11 实测：格式合法的 `__biz` 链接在无用户交互下被 302 到验证页）。见 [ADR-0006](./docs/adr/0006-single-entry-from-article-tab.md)。
- **安装时用户只会看到一条权限警告**，来自 `host_permissions`（`mmbiz.qpic.cn`、`mmbiz.qlogo.cn`）。不为 `mp.weixin.qq.com` 声明 `host_permissions`；不声明 `tabs`、`downloads`。

## Operating Context

**主流程**：在文章页点扩展图标 → 判定页面 → 抓取正文与来源信息 → 在独立的扩展全页里解析、切块、转换 → 写入**候选快照** → （有旧会话时）确认替换 → 逐块清洗 → 选父目录导出。

**三档页面判定**（content script 注入后只承认这三档）：

| 档 | 判据 | 行为 |
| --- | --- | --- |
| 正常文章页 | 能注入且 `#js_content` 存在 | 进入抓取 |
| **微信提示页** | 能注入、无 `#js_content`、`.weui-msg__title` 存在且非空 | **原样转述微信给出的那句中文**，配「回到原文看看」与「重试」 |
| 通用失败 | 其余全部情况 | 统一提示「这个页面上没找到公众号文章正文」 |

判定**不得匹配具体文案**，只判断标题是否存在且非空。**不猜**是删除、违规还是需要登录。验证页（`/mp/wappoc_appmsgcaptcha`）提示用户回到原文自行完成验证，**不代替用户完成**。

**在读不了的页面点图标时**：照常打开/聚焦扩展全页，**不做「什么都不发生」**——点了没反应对零基础用户等同于扩展坏了。有旧会话就直接继续，顶部给一条非模态提示「刚才那个页面上没有公众号文章，你的进度没有被动过」。

**空状态**明确写出三步：用浏览器打开一篇公众号文章 → 等它显示完 → 点工具栏上的 WeTrim 图标。**不把「没有文章」呈现成错误。**

**内容稳定探测**：点击后立即读一次页面。正文存在时做一次稳定性探测——正文文本长度与图片数量在约 **300 ms** 内不再增长即认为稳定，**最多等约 2 秒**。超时则按当前拿到的内容继续，并在顶部提示「文章可能还没显示完整，可以回到原文等它加载完再重新抓一次」。**不假装抓全了。** ⚠️ 这两个阈值尚未实测。

**单实例**：只允许一个有效编辑页。手动复制标签、连续点击、浏览器会话恢复出多个页面时，多余的页面进入只读态并提供「切换到正在编辑的页面」；只读页不写存储、不显示保存状态。

**导出场景**：每次导出都打开目录选择器，写入 `<文章文件名>/<文章文件名>.md` 与同目录下的 `images/`。v1 不保存默认导出目录、不持久化目录句柄。

**验收基准设备**：Chrome 稳定版 + DevTools CPU 4× 降速，作为低端设备的可复现代理。全部性能目标都定在降速档，不用开发机裸测值。

## Capabilities and Constraints

领域术语（**内容块**、**块类型**、**块身份**、**初始内容 / 当前内容**、**还原内容**、**清洗会话**、**候选快照**、**清洗结果**、**图片块 / 图片引用 / 图片资源**、**富媒体块**、**未知内容块**、**转换提示**、**微信提示页**等）以根目录 [`CONTEXT.md`](./CONTEXT.md) 为准，本文件不重新定义。

### 清洗

- 单栏连续阅读，按文章原顺序展开**全部**内容块。块序号、初始**块类型**、保留 / 剔除、已修改、内容为空等状态都有**文字**表达。
- **所有块**都可编辑 Markdown；初始类型不限制当前内容，**编辑不重新切块、不改变初始类型**。编辑自动保存。
- 剔除后在**原位置折叠成摘要**，不丢弃当前内容；可恢复保留、可展开，顶部筛选可集中找回。
- **还原内容**恢复初始内容但**不改变取舍**，还原前确认一次。编辑为空是有效状态，空白块不进入清洗结果。
- 列表、引用、表格**整体取舍**，不提供子块开关；块内图片通过编辑 Markdown 取舍。共享**图片资源**不会因某一个**图片块**被剔除而从其他块消失。
- **转换提示**是界面辅助信息，单独呈现，**不自动混进导出正文**；**富媒体块**的降级信息与**未知内容块**的占位则是可编辑、可取舍的正文。
- 长文导航：全文搜索、块序号定位、全部/保留/剔除筛选。**不做历史文章侧栏。**
- 操作后保持阅读位置与编辑焦点；目标消失时给一个可继续操作的合理焦点。顶部保存状态与主要操作保持可见。
- 检查结果是汇总预览对话框，可切换阅读预览与 Markdown 源码，**仅含保留块的当前非空内容**，与正式导出语义一致。

### 转换

- **每个内容块独立转换，绝不「整篇拼接后再回填占位符」**（[ADR-0007](./docs/adr/0007-per-block-conversion.md)）。上游正是栽在占位符顺序替换上：一篇 40 个代码块的文章只有前 10 个存活。
- 失败分两档：**整篇级**（拿不到正文、解析不出 DOM、切块器整体抛错）走通用提示 + 重试，不写候选、不动当前会话；**单块级**（某块转换抛错，或原始 HTML 非空却转出空内容）降级为**未知内容块**并挂转换提示，候选照常写入。
- **不以空字符串静默丢失，不伪造信息。** 「非空 HTML 转出空」必须主动检查——公众号名片全部内容在属性里，用叶子判据会让它静默消失，比抛错更危险。
- 预览与导出共用同一套 Markdown 方言、当前内容、保留状态、表格降级与富媒体占位规则；唯一差异是导出副本里图片 URL 到本地路径的映射。预览不承诺与外部阅读器像素一致。

### 导出

- 导出到每次选择的父目录下新建的文章目录，结构为 `<文章文件名>/<文章文件名>.md`，本地图片放在同目录的 `images/` 下；**没有本地图片时不创建 `images/`**。
- 重名时依次找 `<文章文件名> (2)`、`(3)` 等未占用名称；**后缀只用于外层目录**，内部 Markdown 文件仍用原清理后的文件名。**不覆盖、不合并、不删除**以前的结果。
- 文件名清理：保留中文与 emoji；把 `/ \ : * ? " < > |` 和控制字符替换为下划线；清除结尾的点与空格；对 Windows 保留名加前缀下划线；清理后为空时用「未命名文章」。基名最多 60 个可见字素，按完整字素截断以免切断 emoji，同时采用不超过 180 UTF-8 字节的保守限制。**完整原始标题仍保存在快照来源信息里，文件名清理不反写文章元数据。**
- 图片集合从**所有保留块当前内容中的有效图片引用**计算，**不依赖初始图片清单，也不看图片块的开关**。同一 URL 只下载一份；保留查询参数，不归一化微信的尺寸、格式或签名参数。
- **保留原始字节不转码**，按**实际媒体格式**选扩展名（不能只凭 URL 后缀或固定 `.jpg`）。返回 HTML 错误页或无法判定为图片的数据按下载失败处理。
- 未本地化的图片（外站、权限缺失、403、网络错误）**默认暂停导出**，显示所在内容、地址与可理解的原因；只有用户**明确选择**「保留外链继续」才继续。**不静默丢图，不把部分离线结果报为完整成功。**
- Markdown 为 UTF-8，顶部 YAML front-matter 固定四字段 `title` / `account` / `date` / `source`，**未知值省略，不输出空占位**。`date` 按 UTC+8 硬编码换算，不随导出设备时区跨日。
- 清洗结果没有非空正文时提示「没有可导出的正文」，**不创建只含来源信息的文件**；不额外自动插入正文一级标题。

### 持久化

- `chrome.storage.local` **三个固定 key**（`currentSession` / `candidateSnapshot` / `pendingCapture`），不按文章建索引，不建历史库。
- **先落盘，再问。** 候选快照写入成功之后才弹替换确认框；确认那一下只是把候选提升为当前会话。**候选永远不会自动提升。**
- 文字编辑防抖 500 ms，且从第一次未保存修改起最长 2 s 内必须发起写入；取舍与还原立即提交。界面持续区分**正在保存 / 已保存 / 最新更改未保存**。**性能优化不得以拉长保存间隔换取流畅度。**
- 写入串行化 + 修订号校验：只有最新修订保存成功才显示「已保存」；清除或替换后旧会话的迟到写入不得复活。**导出成功不能把未保存状态改成已保存。**
- 重开扩展全页**直接恢复**上次成功保存的内容与取舍，**不再询问**，不重新抓取、不重新切块、不重新分配块身份。**图片按保存地址重新获取**，失败显示占位与重试，不阻断文字编辑、不删除引用。
- 恢复边界是**最后一次成功保存的记录**，不承诺断电零丢失。
- 损坏记录**保留原记录、不静默清空**，提供三个出口：下载原始备份（仅供排查）、清除并重新开始（需明确确认）、暂不处理。
- 声明 `unlimitedStorage` 解除 10 MiB 配额，但**仍要处理保存失败**——无限配额不等于保存必成功。

### 长文性能契约

- 按**微信公众号平台上界量级**设计：约 2 万字、**约 600 块**、数十张图。该块数是真实样例（11,986 字 / 347 块，35 字/块）对早期「约 300 块」推断的**修正**。
- ⚠️ 「2 万字上界」本身来自二手且互相冲突的资料，未经微信官方确认。方案写成「按此量级设计，实测后修订」，**不得写成「保证支持 2 万字」**。
- 4× 降速档目标：载入到可交互 ≤ 1.5 s；单块取舍 ≤ 100 ms；逐字编辑 ≤ 50 ms；开关单块编辑器 ≤ 100 ms；预览生成 ≤ 300 ms；搜索/筛选 ≤ 100 ms；自动保存 ≤ 200 ms 且不阻塞输入。图片呈现只给行为目标，不给时间目标。
- **连续渲染全部内容块，不做虚拟列表，也不做分段延迟呈现**（[ADR-0005](./docs/adr/0005-continuous-rendering-without-virtualization.md)）。接缝要留：全部块的渲染入口收敛到单一 `BlockList` 组件边界，搜索、定位与焦点管理必须经由它暴露的接口，**不得绕过它访问 DOM**。
- **不设数量或大小的硬上限，任何长度的文章都不拒绝清洗。** ≥ 1000 块只在渲染前给一次提示，阈值可调，**只提示不拒绝**；正常情况下这条提示永远不会触发，它是安全网不是活跃功能。

### 权限与上架

manifest 声明的权限就是这一组，**不多不少**：

| 条目 | 理由 | 安装警告 |
| --- | --- | --- |
| `activeTab` | 用户点击图标后临时获得当前标签页主框架的主机权限，正好匹配唯一的入口设计 | **无** |
| `scripting` | 注入 content script 所需 | **无** |
| `storage` | 持久化**清洗会话** | **无** |
| `unlimitedStorage` | 解除 `storage.local` 的 10 MiB 默认配额 | **无** |
| `host_permissions`：`*://mmbiz.qpic.cn/*`、`*://mmbiz.qlogo.cn/*` | 跨域取**图片资源**的字节 | 「读取和更改你在 mmbiz.qpic.cn、mmbiz.qlogo.cn 上的数据」 |

**明确不声明**：`downloads`（用 File System Access API 写盘，不需要任何权限声明，避开「管理您的下载内容」警告）、`tabs`（判定页面是否可读靠「尝试注入并捕获失败」）、`declarativeNetRequestWithHostAccess`（已抓取实际请求头证实 mmbiz 不需要 `Referer`，扩展页也不发送，见「已实测数字」）、`mp.weixin.qq.com` 的 `host_permissions`（正文读取只靠 `activeTab`）。

- **用户在安装时只会看到一条警告**，来自 `host_permissions`。
- 上架材料中隐私政策 URL、数据收集披露与有限使用认证必须提交，**逾 30 天不填会被下架**。首次提交会命中「新开发者 + 新扩展」的审核拖慢信号，要预留时间。

### 明确不做（v1 终点之外）

本地文章库 / 历史管理 · 批量抓取与排队 · 粘贴 URL 加载文章 · Firefox 支持 · Python CLI / Camoufox / 本地 Node 服务 · 块的拖拽排序 · Electron / Tauri 桌面应用 · ZIP 导出 · 离线图片缓存 · 虚拟列表 / 分段延迟呈现 · 备份导入。

每一条都是**已判定**在范围之外，不会因为「顺手」而回到范围内。

## Brand Commitments

- 名称 **WeTrim**。商店描述「手动清洗微信公众号文章，导出 Markdown 和图片。」，图标默认标题「用 WeTrim 清洗这篇文章」。
- **界面语言中文优先，预留 i18n**：v1 只发中文；文案集中管理、不硬编码进组件，便于后续加语言。面向开发者的文档与 commit message 同样使用中文。
- 文案纪律（已由决策确定，不是风格偏好）：原样转述微信给出的原因，**不推断**删除、违规、登录等具体原因；不伪造信息；不用空字符串掩盖内容丢失；不把失败报成成功；不把「没有文章」呈现成错误。
- 上架素材（图标、商店截图、隐私政策 URL、数据收集披露与权限用途说明）**尚未制作**。隐私政策与有限使用认证逾 30 天不填会被下架。

## Evidence on Hand

### 可用于验证的真实文章

十篇真实公众号文章，按用途分三组。实现期的验证直接用这些 URL，不必再「按标准去找」。

**端到端可行性**（3 篇，含正文内 `mp-common-profile` 公众号卡片；文字结构与真实图片 URL 顺序在滚动前后完全一致）

- <https://mp.weixin.qq.com/s/4oW2sWIYTpoTc58FhQWWhQ>
- <https://mp.weixin.qq.com/s/q8otur4qi_-6Gc13vBE7ZQ>
- <https://mp.weixin.qq.com/s/pKl5Ku7C50eUnBUAmPNf5A>

**长文性能**（3 篇）

| 维度 | 文章 | 字数 | 块数 | 正文 HTML | DOM 元素 | 图片 |
| --- | --- | --- | --- | --- | --- | --- |
| 深度长文 | [每10年换一副骨架…](https://mp.weixin.qq.com/s/bFIyu9ssse5vOqi6sEHVYw) | 11,986 | **347** | **295.7 KB** | **5,569** | 6（全 PNG，**9.50 MB**） |
| 图集 | [神经根型颈椎病的诊疗思路与治疗](https://mp.weixin.qq.com/s/8EIed5cmXFIB2uqxDg3CYg) | 1,283 | 67 | 122.9 KB | 323 | **22**（3.90 MB） |
| 复合结构 | [Advanced Science｜骨修复材料的下一步](https://mp.weixin.qq.com/s/l-CVnH-C0hO4nTQX1MNa4g) | 3,625 | 64 | 81.2 KB | 565 | 10（3.16 MB） |

块类型分布：深度长文 段落 322 / 表格 19 / 图片 6；图集 段落 43 / 图片 22 / 富媒体 1 / 列表 1；复合结构 段落 46 / 图片 10 / 标题 6 / 表格 2。三篇最大 347 块，均未触发 1000 块提示。

**结构覆盖**（4 篇）

| 文章 | 贡献的结构 |
| --- | --- |
| [从光学鼠标到企业 AI…](https://mp.weixin.qq.com/s/SR9YbSi_VXFwtC4Cf-hsMw) | 第三方 `pre > code` 代码块 |
| [Agent 长程任务断点续传…](https://mp.weixin.qq.com/s/Vy2HpOyr7wVmPTVWa3mGig) | 3 个有序列表、7 个官方代码块、44 处行内代码 |
| [48小时数百万浏览：Graph Engineering…](https://mp.weixin.qq.com/s/-y9Gqkckr1AJsuNReEKZYg) | 引用、无序列表 |
| [AI 开始批准代码合并了](https://mp.weixin.qq.com/s/nFgdzgL9kWXdYAfmu7gvPQ) | 富媒体卡片、行内代码 |

四篇里所有结构的切块行为全部符合 [ADR-0001](./docs/adr/0001-content-block-boundaries.md)。

### 已实测数字

- 最大正文 HTML 295.7 KB / 5,569 DOM 元素 / 347 块；三篇真实文章的最大会话载荷 393 KB。
- 合成探针：100 / 300 / 600 块载荷分别为 80 KB / 238 KB / 473 KB；300 块挂载 670 ms、600 块 850 ms；JS 堆全程个位数 MB。
- 300 块 / 1.87 万字 / 30 图在 4× 降速档：载入到可交互 670 ms，取舍与逐字编辑低于一帧，预览生成 19.4 ms。
- 滚完全篇每步 p95 34 ms；600 块 / 60 图、页面高 126,879 px 时最差 39.2 ms，未观察到掉帧。
- 内存由图片解码主导但随规模饱和：30 图 +202 MB，60 图 +223 MB。
- **网络成本由格式与单张体积驱动，与张数无关**：6 张全 PNG 的深度长文合计 9.50 MB（单张中位 1,623 KB），22 张混合格式的图集只有 3.90 MB。图片最少的一篇反而是最贵的那篇的 2.4 倍——**导出进度反馈不能按张数估。**
- 序列化与写入：300 块整篇序列化 2.9 ms、写入 2.7 ms，600 块合计不到 10 ms（⚠️ 写入为代理值，见下）。
- **生产构建第三方库 eval 依赖与 MV3 CSP 兼容性**（Issue #15 实测）：React 19、Turndown 7.2.4、`turndown-plugin-gfm` 1.0.2、marked 15.0.12、DOMPurify 3.4.15 的生产构建打包进入 MV3 扩展后，在 Chrome 153 默认严格 CSP（`script-src 'self'; object-src 'self'`，无 `unsafe-eval`）下真实调用运行（React 渲染、Markdown 转 HTML、DOMPurify 清洗、HTML 表格经 Turndown+GFM 转 GFM 语法表），0 次 CSP 违规、0 次报错。
- **页面抓取内容稳定探测 300 ms / 2 s 阈值校准**（Issue #16 实测）：在微信公众号页面 DOM 与真实文章样例上实测：在页面加载完成时（正常用户点击图标场景），DOM 无文字长度与 `img[data-src]` 变动，探测器在 300 ms 静止窗口后精准判定就绪（实测平均 301~302 ms，unstable = false）；在动态增量渲染场景中，探测器能在 50 ms 采样间隔下灵敏捕获内容增长并重置静止计时，在增量平息后 300 ms 判定就绪（实测 503 ms，unstable = false）；在持续变动场景中，探测器在达到最大硬超时上限（2,000 ms）后立即停止并标记 unstable = true，耗时受控，300 ms / 2,000 ms 阈值校准合格。
- **列表与嵌套编码实测**（Issue #20 实测）：四篇真实样例中 12 个内容列表（长列表篇 8 个、引用列表篇 4 个）全部通过实测验证，3 个有序列表与 9 个无序列表完全同构转换；微信官方多级列表确认采用真嵌套 `<ul><li><ul>`（假设 A），第三方可能产生平铺兄弟列表 + `list-paddingleft-N`（假设 B）；WeTrim 在切块前合并相邻兄弟列表（中间无其它内容），按 `paddingleft-N` 重构为嵌套树，实测使假设 A 与假设 B 均稳定生成 1 个符合 ADR-0001 的列表块；确认并记录固有降级（连写两个独立一级列表且中间无内容时在平铺下并为 1 个块）；全量 11 篇基准样例文档中的 16 个内容列表块的 Markdown 均自检无多余空行。
- **长文全文搜索与筛选切换 4× CPU 降速实测**（Issue #29 实测）：在 Chrome 153 下设置 CDP `Emulation.setCPUThrottlingRate: 4` 降速档进行端到端基准实测，预算指标 ≤ 100 ms：
  - 真实深度长文（347 块，5,569 DOM 元素）：关键词检索到命中计数更新及 CSS Custom Highlight 高亮建立 **1.0–2.7 ms**（28 处命中）；切换三态筛选（全部/保留）**2.4–14.8 ms**。
  - 合成大规模长文（600 块合成段落与标题）：关键词检索到命中计数及高亮建立 **2.0–4.8 ms**（103 处命中）；切换三态筛选 **0.2–22.8 ms**。
  - 以上为切换筛选时保持阅读位置的修复（`71b1d75`）之后，`test/verify-issue29.mjs` Test 12 连跑 7 轮的范围，每轮每项只采一个样本，单次波动可达数倍。
  - 筛选切换的口径是「调用 `setFilter` 到下一帧 `requestAnimationFrame` 回调」，不保证已包含 React 提交与布局（0.2 ms 一类的值即属此情形），**不能当作「到渲染完成」的实测**；它只说明最差样本仍远低于 100 ms 预算。
  - 证实了 CSS Custom Highlight API 免 DOM 改动的性能优势与 `BlockPlainTextCache` 纯文本按内容缓存机制，各项指标均远低于 100 ms 感知预算。
- **微信图片的 `Referer` 与防盗链占位图**（Issue #30 实测，Chrome 154）：
  - 直接请求真实 mmbiz 图片：不带 `Referer` 或带 `https://mp.weixin.qq.com/` 时返回真图（200 `image/png`，1,662,001 字节，945×1265）；带外站或 `chrome-extension://` 的 `Referer` 时返回 **200 的 140×140 JPEG 占位图**（2,090 字节，「此图片来自微信公众平台 未经允许不可引用」），`onerror` 不会触发。
  - 用 CDP 抓取扩展全页的实际请求头：清洗页 `<img>` 与导出用的 `fetch()` 都**不发送 `Referer`**，拿到的都是真图。为不依赖浏览器默认行为，`app.html` 显式声明 `<meta name="referrer" content="no-referrer">`。
  - 回归检查为 `npm run verify:image-referer`（依赖外网；`npm run verify:image-referer -- --force-referer=https://example.com/` 可确认它能识别出占位图）。

### 现在不得当作已验证

以下每一条今天都**没有**实现层面的证据。任何后续文档、提交说明或票据都不得把它们记为已验证。

| 条目 | 现状 |
| --- | --- |
| `chrome.storage.local` 真实写入耗时 | 上面的 2.7 ms 是同样由 LevelDB 支撑的 **IndexedDB 代理值**，缺少扩展页到浏览器进程的一次 IPC |
| Turndown 转换 347 块的耗时 | **完全没测过**。深度长文每块约 16 个元素（其中 4,234 个 `span`），比合成样例杂乱得多；4× 降速下「百毫秒级」是**估算不是实测** |
| 单块交互的真实耗时 | 取舍、开关编辑器、逐字输入测得的值全部等于双帧等待的量化地板，只能得出「低于一帧」 |
| 「内存随图片数饱和」 | 只有 30 / 60 张**合成图**的证据。真实图片解码尺寸已证明同量级，但饱和结论本身未在真实数据上验证 |
| `mmbiz.qlogo.cn` 是否真的用得上 | 未确认。能少一个域名就少一点警告面 |
| 引用套列表 / 多段落引用 / 公式 / 空 `js_darkmode` `<pre>` 噪声 | **无真实样本**，结论全部来自手工构造的 HTML |
| 微信提示页的文案种类 | **只观察到「参数错误」一种**。其余文案落在同一模板里是推断，无真实样本——这正是判定只看文案是否非空、口径只做转述的原因 |

| 商店安装时的实际权限提示 | 观察的是「加载已解压扩展」，**不能推广为商店安装** |
| 跨会话免授权 / 目录句柄持久化 | 选择器记住上次位置**不等于**句柄持久化或免授权。v1 本就不持久化句柄 |


### 尚不存在的东西

上架素材（图标、商店截图、隐私政策 URL、数据收集披露、权限用途说明）尚未制作。没有用户测试、没有竞品基准、没有真实用户反馈——**不得虚构**。

## Product Principles

1. **零安装门槛优先。** 它是唯一的硬约束；任何需要用户装点什么、配点什么的方案，在这里都不成立。
2. **先落盘，再问。** 每一步失败都不得破坏已有的清洗会话——获取、解析、保存全部成功之后，用户拍板那一下才只是把候选提升为当前。
3. **不伪造、不静默丢失。** 降级必须可见：转换降级给提示，图片失败给占位和重试，读不到正文就转述原因而不猜测原因。
4. **一次一篇，逐块由用户拍板。** 工具不替用户判断该留什么，也不做批量、历史库和自动化。
5. **区分已决定、已实测、尚未验证，并且从不混用。** 地图推进中已有两次推论被后续实测推翻，这条纪律的价值一半在于不让第三次发生。

## Accessibility & Inclusion

**键盘可达 + 焦点保持是硬要求**：全部取舍、编辑、筛选、还原、导出操作都必须能纯键盘完成；操作后保持阅读位置与编辑焦点，过滤、取舍、完成编辑都不得无故跳顶；目标消失时给一个可继续操作的合理焦点。

这条同时是架构约束——焦点管理必须经由 `BlockList` 暴露的接口，不得直接查找 DOM 节点。

未确立更高标准：v1 **未**承诺对齐 WCAG 2.1 AA 的完整条款（对比度、可见焦点环、语义标签等）。这是有意留空的决定，不是遗漏。
